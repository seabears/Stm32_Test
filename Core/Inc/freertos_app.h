#ifndef FREERTOS_APP_H
#define FREERTOS_APP_H

/* Creates the application tasks and starts the scheduler. This function only
 * returns when task creation or scheduler startup fails. */
void FreeRTOSApp_Start(void);

#endif /* FREERTOS_APP_H */
